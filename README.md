A simple command-line application that fetches the latest news articles using the News API.
The program takes a search query and API key as input and displays recent news articles directly in the terminal.

Prints neatly title, link of that article and some description.
